
name: new
on: push
jobs:
  upload1:
    name: uploading office files to server
    runs-on: ubuntu-latest
    environment: test
    steps:
    
      - name:  Checkout Repository from GitHub      
        uses: actions/checkout@v2
        
      - name: test
        #if : {{ jobs.upload1.environment == test1 }}
        run: |
          echo ${{ jobs.upload1.environment }}

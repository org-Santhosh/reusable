
name: samosa_workflow
on: push
jobs:
  upload1:
    runs-on: ubuntu-latest
    steps:
      - name: Step 1 - Checkout Repository from GitHub
        uses: actions/checkout@v2
        
      - shell: bash
        run: |
          ls
          echo "***************"
          pwd
          echo "***************"
          pwd
          echo "***************"
          ls /home
          pwd
          echo "***************"
          ls /home/runner
          pwd
          echo "***************ls -R /home/runner/work "
          ls -R

          echo "***************"
          ls /home/runner/work/_PipelineMapping

          echo "***************"
          ls /home/runner/work/_actions

          echo "***************"
          ls /home/runner/work/_temp
